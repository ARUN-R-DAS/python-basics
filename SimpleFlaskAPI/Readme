Simple flask api with streamlit interface
---------------------------------------
- Run both server and client in terminal
- Input a string in client and press button
- The server return a modified string and visible in client page.
